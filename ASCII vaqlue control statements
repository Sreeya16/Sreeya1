#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    char n;
    scanf("%c",&n);
    if(n>=65&&n<=90)
    {
        printf("Upper");
    }
    else if(n>=97&&n<=122){
        printf("Lower");
    }
    else if(n>=48&&n<=57)
    {
        printf("Number");
    }
    else
    {
        printf("Symbol");
    }
    return 0;
}
